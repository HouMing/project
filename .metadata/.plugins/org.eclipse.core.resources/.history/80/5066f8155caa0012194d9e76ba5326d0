package org.wave.mvc.m.dao;

import org.springframework.data.mongodb.core.MongoTemplate;
import org.wave.jpa.Band;
import org.wave.mvc.exception.DAOCreateException;
import org.wave.mvc.exception.DAODeleteException;

public abstract class DAO<T>
{
	void create(T obj)
	{
		if (obj == null) {
			throw new DAOCreateException("缺少说明");
		}
		getMongoTemplate().insert(obj);
	}

	void delete(T obj)
	{
		if (obj == null) {
			throw new DAODeleteException("缺少说明");
		}
		getMongoTemplate().insert(obj);
	}

	public abstract MongoTemplate getMongoTemplate();
}
